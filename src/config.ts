// Configurações globais da aplicação
export const config = {
  api: {
    baseURL: 'http://localhost:5000',
    timeout: 10000,
  },
  app: {
    name: 'Odonto System',
    version: '1.0.0',
  },
} as const
